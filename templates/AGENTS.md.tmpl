# {{SUBPROJECT_NAME}} -- AI Assistant Guide

<!-- 
  This is a per-directory AGENTS.md template.
  Place one in each major subdirectory (apps/, packages/, services/) 
  to give AI assistants directory-specific context.
  
  CLAUDE.md (root) provides the global view.
  AGENTS.md (per-directory) provides the local view.
-->

## Critical Rules

<!-- List 3-5 non-negotiable rules for this part of the codebase -->

1. <!-- Rule 1: e.g., "Never replace state wholesale -- always spread-merge" -->
2. <!-- Rule 2: e.g., "CSS variables for theming -- never hardcode colors" -->
3. <!-- Rule 3: e.g., "All API calls go through client.ts -- no raw fetch" -->

---

## Architecture

```
src/
├── ...    # Describe the directory structure
└── ...
```

---

## Key Patterns

<!-- Show the most important code patterns with correct/incorrect examples -->

### [Pattern 1 Name]

```typescript
// CORRECT
```

```typescript
// WRONG -- explain why
```

### [Pattern 2 Name]

```typescript
// CORRECT
```

---

## Common Operations

### [Operation 1: e.g., "Adding a New Component"]

1. Step 1
2. Step 2
3. **Verify**: [command to verify]

### [Operation 2: e.g., "Adding an API Endpoint"]

1. Step 1
2. Step 2
3. **Verify**: [command to verify]

---

## Verification

Before marking work in this directory as complete:

```bash
# Run the appropriate check command
```
